# Deforest

This repo keeps **code only**. Large datasets are downloaded on demand.

## Data layout

- Put dataset files in `data/` (ignored by git).
- Define what to download in `data/manifest.tsv` (tab-separated):

